# Commercial V2 CP6 Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the Commercial V2 CP6 candidate code-correct and release-gated, then validate the real commercial loop only on `quickiching-staging`.

**Architecture:** Keep the existing Commercial V2 architecture. Repair correctness at existing boundaries: readiness, middleware, PostgreSQL loaders, entitlement reservation/finalization, durable payment outbox, AI timeout, reconcile budgeting, pricing, and CI gates. Fail closed where durable PostgreSQL state or integrity evidence is required.

**Tech Stack:** Next.js 15, TypeScript, Vitest, Bun 1.3.14, PostgreSQL, Drizzle ORM/migrations, Better Auth, Waffo Test, Vercel Workflow, Vercel deployment.

**Spec:** `docs/superpowers/specs/2026-08-28-cp6-repair-design.md`

## Global Constraints

- Source branch must start at `07a168d219e134dadffdd8c2ed3ca0e3bef0bc95`.
- Work on `codex/commercial-v2-cp6-repair` only until final PR update.
- Never modify `drizzle/0000_*` through `drizzle/0010_*`.
- Any schema delta uses a new `0011_*` or later migration.
- No staging migration/deploy until code and CI gates are green.
- Vercel staging project is `quickiching-staging`; never touch `ichingcoin`.
- Production remains NO-GO and no merge is permitted.

---

### Task 1: Commercial readiness and migration integrity

**Files:**
- Modify: `src/server/readiness/readiness-service.ts`
- Modify: `src/server/readiness/readiness-service.test.ts`
- Create: `src/server/readiness/migration-integrity.ts`
- Create: `src/server/readiness/migration-integrity.test.ts`
- Create: `scripts/check-migration-integrity.mjs`
- Create: `drizzle/meta/migration-integrity.json`
- Modify: `src/app/api/ready/route.ts`
- Add/modify route test if an existing `src/app/api/ready/*.test.ts` is present; otherwise create `src/app/api/ready/route.test.ts`.

**Interfaces:**
- `EXPECTED_COMMERCIAL_MIGRATIONS`: ordered immutable entries `{ idx, tag, createdAt, hash }`.
- `queryMigrations(): Promise<Array<{ createdAt: number; hash: string }>>` on readiness DB override.
- `checkSystemReadiness()` returns detailed internal readiness while `/api/ready` serializes only `{status, overall}`.

- [ ] Extend the existing readiness red tests so partial capability enablement, any missing one of 24 tables, missing migration rows, out-of-order rows, and wrong migration hashes all fail.
- [ ] Verify those tests fail against the current implementation.
- [ ] Add the ordered hash manifest and integrity helper. The helper must require contiguous journal indexes, exact tag order, and SHA-256 equality for every SQL migration.
- [ ] Change readiness DB inspection from timestamp-only to ordered `{createdAt, hash}` rows from `drizzle.__drizzle_migrations` / the configured Drizzle migrations schema.
- [ ] Replace the 11-table constant with all 24 database tables represented by `databaseSchema`.
- [ ] Require `COMMERCIAL_CAPABILITIES.every(cap => requested && enabled)` rather than `commercialEnabled`/any-enabled semantics.
- [ ] Keep detailed report internal but change public `/api/ready` payload to coarse status only.
- [ ] Run unit tests, migration integrity script, lint, typecheck, and build; commit.

### Task 2: Account route and PostgreSQL loader fail-closed behavior

**Files:**
- Modify: `src/middleware.ts`
- Modify: `src/middleware.test.ts`
- Modify: `src/server/loaders.ts`
- Create/modify loader tests following existing Vitest module-mocking patterns.

**Interfaces:**
- Exact allowed API path: `/api/account/delete` only when Auth capability is enabled.
- In PostgreSQL mode, database read failures propagate; no repository fallback is allowed.

- [ ] Add a middleware test proving `/api/account/delete` is 200 passthrough with complete Auth config, remains 404 when Auth is disabled, and `/api/account/*` unregistered siblings remain 404.
- [ ] Verify red.
- [ ] Add loader tests that force PostgreSQL query failure for history and entitlement balance and assert rejection instead of synthetic empty/zero values.
- [ ] Verify red.
- [ ] Add the exact middleware gate before the generic `/api` deny rule.
- [ ] Remove PostgreSQL-mode catch-and-fallback behavior in `loadHistory()` and `loadEntitlementBalance()` while preserving in-memory behavior when PostgreSQL mode is not selected.
- [ ] Run focused tests and full unit suite; commit.

### Task 3: Entitlement reservation expiry and Deep Reading final safety fence

**Files:**
- Modify: `src/server/generation/deep-reading-service.integration.test.ts`
- Modify: `src/server/generation/deep-reading-service.ts`
- Modify/create Deep Reading workflow integration tests covering `finalizeDeepReadingStep`.
- Modify: `src/server/workflows/deep-reading-steps.ts`

**Interfaces:**
- Reservation `expires_at` equals selected batch `expires_at`.
- Initial reservation `lease_expires_at = min(batch.expires_at, now + lease duration)`; initial lease token remains bound to that lease.
- Finalization verifies lifecycle, risk, and cast-result HMAC before any output persistence or entitlement consumption.

- [ ] Add an integration test with a near-expiry batch and assert reservation expiry inherits the batch expiry and lease expiry never exceeds it.
- [ ] Verify red.
- [ ] Select `expires_at` with the batch, persist it into the reservation, and set bounded initial lease expiration.
- [ ] Add finalization tests that mutate lifecycle, mutate risk status, and tamper `result_hmac` after job claim; each must reject and leave reservation unconsumed/result absent.
- [ ] Verify red.
- [ ] Re-run lifecycle/risk checks inside the final locked transaction.
- [ ] Build a `PreviewGenerationContext` from the locked session/facts and call `createResultIntegrityVerifier()`; reject with `CAST_RESULT_INTEGRITY_INVALID` before output persistence/credit consumption when verification fails.
- [ ] Run focused PostgreSQL integration tests; commit.

### Task 4: Refund-before-order durable retry behavior

**Files:**
- Modify: `src/server/payments/outbox-dispatcher.integration.test.ts`
- Modify: `src/server/payments/postgres-repository.ts`
- Modify if required: `src/server/payments/outbox-dispatcher.ts`

**Interfaces:**
- `pending_order` must set a future `payment_outbox.available_at` so `dispatchAllPending()` cannot reclaim the same row in the same loop.
- Any unresolved/dead-letter refund for an order must prevent later payment grant from issuing usable credits without review.

- [ ] Add a test where a refund arrives before the order and `dispatchAllPending({maxBatches:5})` runs; assert exactly one attempt is consumed and `available_at` is in the future.
- [ ] Add a test seeding an early refund in dead-letter/unresolved state, then processing payment; assert payment enters financial review and no available entitlement remains/grants silently.
- [ ] Verify red.
- [ ] Add a bounded pending-order retry delay when returning to `pending` state from missing/unpaid order paths.
- [ ] Broaden payment preflight to detect unresolved refund states, including dead-letter, and fail closed to financial review rather than grant.
- [ ] Run payment PostgreSQL integration suite; commit.

### Task 5: AI timeout and reconcile budget

**Files:**
- Modify: `src/server/workflows/deep-reading-steps.ts`
- Modify AI/workflow tests.
- Inspect and modify the current reconcile service/route implementation and its tests.

**Interfaces:**
- AI calls receive an AbortSignal that aborts after a configured bounded duration and is cleaned up.
- Reconcile stops before starting additional outbox work once budget is exhausted and bounds each unit of work to remaining budget where supported.

- [ ] Add fake-timer/unit tests proving generation and review signals abort when timeout elapses.
- [ ] Verify red.
- [ ] Use `AbortSignal.timeout()` or an equivalent timer-backed controller rather than a never-aborted controller.
- [ ] Add reconcile test with a slow dispatcher item proving the 10-second budget is enforced at item granularity.
- [ ] Verify red.
- [ ] Refactor reconcile to dispatch claimed items with remaining-budget checks rather than one unbounded bulk batch call.
- [ ] Run focused and full tests; commit.

### Task 6: CP6 workflow, pricing, and release gate completeness

**Files:**
- Modify: `.github/workflows/cp6-commercial-v2.yml`
- Modify: `src/app/(default)/pricing/page.tsx`
- Modify/create pricing tests if an existing page/component test pattern exists.

**Interfaces:**
- CP6 workflow runs for the repair branch and PR target used by this checkpoint.
- Application job includes migration integrity, `git diff --check`, build, lint, typecheck, and unit tests.
- PostgreSQL job remains serial and separate.
- Pricing exposes a real checkout entry only when server capability resolution says checkout is enabled; disabled mode remains non-purchasable.

- [ ] Add static/workflow tests if the repository already has workflow-contract tests; otherwise make the YAML change and verify via GitHub Actions.
- [ ] Add build, migration integrity script, and `git diff --check` steps; add repair branch triggers.
- [ ] Replace unconditional “Commercial V2 unavailable” pricing copy with capability-aware purchase entry while preserving safe disabled copy.
- [ ] Run full local-equivalent gates available in CI; commit.

### Task 7: CI verification and Draft PR update

**Files:** no product-code changes unless a gate reveals a root-cause defect.

- [ ] Push/observe GitHub Actions on `codex/commercial-v2-cp6-repair`.
- [ ] Require application and PostgreSQL jobs to pass; inspect failures before any follow-up fix.
- [ ] Require build and `git diff --check` pass.
- [ ] Compare repair branch against `07a168d` and verify no `drizzle/0000_*`–`0010_*` file content changed.
- [ ] Only after green, fast-forward/update the existing CP6 Draft PR head path or create/update the appropriate Draft PR without merging.

### Task 8: Staging migration, deployment, and commercial acceptance

**Files:** deployment/provider/database state only; no production project changes.

- [ ] Identify Vercel project `quickiching-staging` and verify it is distinct from `ichingcoin` before any write.
- [ ] Verify staging environment uses staging PostgreSQL, Waffo Test, test provider keys, and all six Commercial V2 capabilities intentionally enabled.
- [ ] Apply only required forward migrations to the staging database; verify 24 tables and exact Drizzle migration hash sequence.
- [ ] Deploy `quickiching-staging` to its Production target.
- [ ] Verify `/`, `/api/health`, and coarse `/api/ready` statuses.
- [ ] Run test-account login, checkout, Waffo Test webhook, credit grant, AI preview, paid Deep Reading, workflow completion, credit consumption, refund, reconcile/cron, account views, and dedicated test-account deletion last.
- [ ] Record HTTP status, structured result, before/after DB state, and redacted provider event/order identifiers; no screenshots required.
- [ ] Update Draft PR with exact evidence and explicit `PRODUCTION NO-GO` statement.
- [ ] Stop. Do not merge and do not deploy `ichingcoin`.
