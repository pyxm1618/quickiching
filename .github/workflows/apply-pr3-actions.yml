name: apply-pr3-actions

on:
  push:
    branches:
      - agent/v2-1-recovery-lifecycles

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v2-1-recovery-lifecycles
          fetch-depth: 0
      - run: python3 scripts/apply-pr3-actions.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: integrate lifecycle application services"
          git push origin HEAD:agent/v2-1-recovery-lifecycles
