name: ga4-consent-tdd

on:
  push:
    branches:
      - agent/add-ga4-consent
  pull_request:
    branches:
      - agent/rename-quick-i-ching

permissions:
  contents: read

concurrency:
  group: ga4-consent-tdd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run test -- src/lib/analytics-consent.test.ts
